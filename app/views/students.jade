#students
  .title
    .title-headers
      h1 we came to code
      h4 meet the next generation of web developers
  .each-student(ng-repeat='student in students')
    img(src='{{student.pic1}}' data-toggle='modal' data-target='#myModal{{$index}}')
    h2 {{student.name}}

    .modal.fade(tabindex='-1' role='dialog' id='myModal{{$index}}' aria-labelledby='myModalLabel' aria-hidden=true)
      .modal-dialog
        .modal-content
          .modal-header
            button(type='button' class='close' data-dismiss='modal' arai-label='Close'): span(aria-hidden='true')&times;
            h2 {{student.name}}
          .modal-body
            .container-fluid
              .row
                .col-md-6
                  img(src='{{student.pic2}}')
                .col-md-6
                  .row
                    h3: a(href='{{student.portfolio}}'): i.fa.fa-floppy-o &nbsp Portfolio
                  .row.contact-me
                    h3: a(href=' {{student.linkedin}}'): i.fa.fa-linkedin-square
                    h3: a(href='{{student.github}}'): i.fa.fa-github-alt
                    h3: a(href='{{student.twitter}}'): i.fa.fa-twitter
          .modal-footer
            h3 {{student.summary}}
